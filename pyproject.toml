[project]
name = "lateral-reading-skill"
version = "0.1.0"
description = "Codex and Claude Code plugin for cited lateral-reading trust reports."
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
